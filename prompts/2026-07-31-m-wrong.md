# 学生端错题本页（m/wrong.html）提示词

将以下内容完整复制并粘贴到你使用的 AI 工具中。

---

为学生端创建一个错题本页面的单文件 HTML（pages/m/wrong.html）。所有数据为静态数据。使用内联 CSS，原生 JavaScript 实现 Accordion 折叠交互。页面面向移动端 390px 视口。

## 全局
- 移动端容器：`max-width: 390px`，水平居中，100vh，flex-column。
- 主色：`#002045`（牛津蓝），正文字体：`'IBM Plex Sans', sans-serif`。

## 组件（从上到下）

### 1. 顶部标题栏（56px，牛津蓝背景，白色文字）
- 左侧返回箭头（`<` SVG）+ 标题"错题本"（16px，font-weight 600）+ 右侧漏斗形筛选图标 SVG

### 2. 统计卡片行（flex 3 列等宽，gap 8px，padding 16px）
3 张卡片，每张白色背景、1px 边框 `#d4d0c8`、8px 圆角、14px 8px 内边距、flex-column 居中对齐、gap 4px：
- 总错题：**23**（牛津蓝，Cormorant Garamond，28px，bold）+ 灰色标签"总错题"（12px）
- 本周新增：**5**（牛津蓝，同样式）+ "本周新增"
- 已掌握：**12**（甲基绿 `#2c6e49`，同样式）+ "已掌握"

### 3. 错题列表（padding 0 16px，flex-column，gap 8px）
3 张可折叠卡片，每张白色背景、8px 圆角、1px 边框、`overflow: hidden`。

#### 卡片 1（默认展开，概念理解型）— `.wq-card.open`
**头部**（flex 横向，padding 12px，gap 10px，`align-items: center`，`cursor: pointer`）：
- 题号"#1"（14px bold）
- 障碍标签"概念理解型"：紫色 `#7B2D8E` 背景、白色 11px 文字、`border-radius: 4px`、`padding: 2px 8px`
- 题目摘要（flex:1，13px 灰色，单行省略号）："下列物质中，属于电解质的是？"
- 折叠箭头 SVG（向下 = 展开状态，`transform: rotate(180deg)`）

**内容区**（`max-height: 0 → scrollHeight`，`transition: 0.3s ease`）：
- `padding: 0 12px 12px`，flex-column，gap 8px

包含：
- 完整题目（14px，`#fafafa` 背景，10px 12px padding，6px 圆角）："下列物质中，属于电解质的是？\nA. 蔗糖\nB. NaCl溶液\nC. 熔融NaOH\nD. 铜"
- 错误答案行（红色背景 `#fff0f0`，8px 12px，4px 圆角，13px font-weight 500，红色 `#c0392b`）："✗ 你的答案：B"
- 正确答案行（绿色背景 `#f0fff0`，同样式，绿色 `#2c6e49`）："✓ 正确答案：C"
- 解析文字（13px 灰色，line-height 1.6）："电解质定义：在水溶液或熔融状态下能导电的化合物。A 蔗糖是非电解质；B NaCl溶液是混合物不是化合物..."
- 双按钮行（flex gap 8px，margin-top 4px）：
  - "生成变式题"：深青 `#13696a`，白色文字，flex-grow，40px 高，8px 圆角
  - "已掌握"：牛津蓝 `#002045`，白色文字，flex-grow，40px 高，8px 圆角

#### 卡片 2（默认折叠，审题障碍型）
- 头部："#2" + 障碍标签"审题障碍型"（蓝色 `#3B5BA5` 背景）+ 摘要"关于化学键类型的判断，下列说法正确的是..." + 折叠箭头（向右=折叠）
- 内容（折叠隐藏）：完整题目（化学键判断题）、错误答案（选 A）、正确答案（C）、解析（NaOH 既含离子键又含共价键）、双按钮

#### 卡片 3（默认折叠，表述障碍型）
- 头部："#3" + 障碍标签"表述障碍型"（青色 `#00897B` 背景）+ 摘要"用化学方程式表示碳酸钠与盐酸的反应..." + 折叠箭头（向右）
- 内容（折叠隐藏）：完整题目（方程式书写）、错误答案（NaCO₃ 化学式错误）、正确答案（Na₂CO₃ + 2HCl → 2NaCl + H₂O + CO₂↑）、解析、双按钮

## 底部 Tab 栏（56px）
- "错题"为激活态（牛津蓝）
- 4 个 tab：AI助教 / 练习 / 错题(激活) / 我的

## JavaScript
```javascript
function toggleCard(header) {
  var card = header.parentElement;
  var content = card.querySelector('.wq-content');
  if (card.classList.contains('open')) {
    card.classList.remove('open');
    content.style.maxHeight = '0';
  } else {
    card.classList.add('open');
    content.style.maxHeight = content.scrollHeight + 'px';
  }
}
```
- 页面加载时 IIFE 初始化默认展开卡片的 `max-height`。
- 箭头旋转：`.wq-card.open .wq-arrow { transform: rotate(180deg); }`。

## 设计约束
- 障碍标签颜色：概念=紫 `#7B2D8E`、审题=蓝 `#3B5BA5`、表述=青 `#00897B`
- 卡片折叠/展开动画 0.3s ease

## 输出
返回完整的 `pages/m/wrong.html` 文件。可直接在浏览器中打开，无需构建步骤。
