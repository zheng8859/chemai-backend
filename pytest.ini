[pytest]
markers =
    l1: L1 单元/规则引擎评测 (基线层, 每次 commit, <5秒)
    l2: L2 API 集成评测 (基线层, 每次 commit)
    l3: L3 AI 内容质量评测 (回归层, 每次 PR)
    slow: L3 慢速测试, 需真实 LLM 调用, 默认跳过 (需 --run-slow 启用)
addopts = -v --tb=short --strict-markers -p no:warnings
timeout = 300
testpaths = tests
asyncio_mode = auto
